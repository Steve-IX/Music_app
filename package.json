{
  "name": "musicstream",
  "version": "1.0.0",
  "private": true,
  "description": "MusicStream - Production-Ready Music Streaming Platform",
  "author": "CodeSmith-Maestro",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0 <22.0.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "dev": "cd apps/web && npm run dev",
    "build": "cd apps/web && npm install && npm run build",
    "preview": "cd apps/web && npm run preview",
    "vercel-build": "npm install && cd apps/web && npm install && npm run build",
    "vercel-install": "npm install"
  },
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ]
} 